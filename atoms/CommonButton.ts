import styled from 'styled-components';
import { Button } from 'antd';

export default styled(Button)`
  position: absolute;
  bottom: 32rem;
  left: 0;
  width: 100%;
  height: 44rem;
  font-size: 15rem;
  font-weight: 600;
  border: 0;
`;
